
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="/stylesheets/output.css" rel="stylesheet">

</head>
<body>

<div class="w-full min-h-screen bg-zinc-900 text-white ">

    
  <div class="form p-3">

    <form action="/create" method="post" class="flex flex-col gap-4">
        <input name="title" type="text" placeholder="Enter title.." class="bg-zinc-700  outline-0 rounded-2xl p-2">
        <textarea name="description" id=""  class="bg-zinc-700 resize-none rounded-2xl min-h-30 outline-0 p-2" placeholder="Enter description here.."></textarea>
        <button type="submit" class="p-2 rounded-2xl max-w-35 cursor-pointer active:scale-[0.9] duration-[0.2s] bg-blue-600">Create task</button>
    </form>

  </div>



  <div class="tasks p-3 flex gap-3 flex-wrap">

    <% if(files.length > 0 ) { %>

        <% files.forEach((val) => { %>
        <div class="task bg-zinc-700 w-82 px-4 py-3 rounded-[10px]">
            <h1 class="text-[20px] tracking-tighter"> <%= val%> </h1>
            <div class="flex justify-between items-center">
                <a href="file/<%= val %>" class="text-blue-400 text-[15px]">Read more</a>
                <div class="flex justify-center items-center gap-5">
                    <a href="edit/<%= val %>" class="text-zinc-300 text-[15px]">Edit filename</a>
                    <a href="delete/<%= val %>" class="text-zinc-300 text-[15px]">
                        <svg xmlns="http://www.w3.org/2000/svg" width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-trash-icon lucide-trash"><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/><path d="M3 6h18"/><path d="M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
                    </a>
                </div>
            </div>
        </div>
        <% })%>

    <% } else{ %>

        <h5 class="text-zinc-500">no task yet.</h5>

    <% }%>

  </div>


</div>

</body>
</html>